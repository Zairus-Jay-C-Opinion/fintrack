# Copy this file to .env and paste your Finnhub key (free at https://finnhub.io/register)
# Never commit .env — it is gitignored.
#
# Local dev: Vite reads this for /api/quote
# Vercel: add FINNHUB_API_KEY in Project → Settings → Environment Variables (not VITE_*)
FINNHUB_API_KEY=
