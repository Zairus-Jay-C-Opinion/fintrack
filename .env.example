# Template only — NO API key here. Safe to commit to GitHub.
#
# You do NOT need this file on Vercel. Set FINNHUB_API_KEY in Vercel instead (see README).
#
# Only for optional local dev: copy to .env and paste your key ( .env is gitignored ).
FINNHUB_API_KEY=
